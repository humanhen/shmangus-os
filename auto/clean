#!/bin/sh
# Remove everything generated by a build, keeping only tracked config.
set -e

lb clean noauto "${@}"

rm -f config/binary config/bootstrap config/chroot config/common config/source
rm -f config/package-lists/live.list.chroot
find config/hooks -type l -delete 2>/dev/null || true
rm -f build.log
